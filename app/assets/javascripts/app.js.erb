angular
  .module('rollingStartAngularJS', ['ngRoute'])
  .config(['$routeProvider', function($routeProvider) {
    $routeProvider.when('/', {
      templateUrl: '<%= asset_path "home/index.html" %>',
      controller: 'HomeCtrl'
    });
  }]);